# INTERNAL NOTES | 零 TVL 修复与 Table 4 重算

本次模型 run：20260919_fm_sequential_h1_h4_h8_h12。核验日期：2026-09-21。复用已经保存的模型预测，没有训练、没有更换 checkpoint、没有强制将模型预测归零。

## 结论

统一损失上限后，全量共同节点评估的模型总体 MAE 仍高于沿用当前图的基线；在每个 horizon 中基线误差最大的 20% 样本里，模型平均 MAE 更低。h12 的尾部优势仅 0.011 个百分点，接近持平。未做独立随机种子重复或显著性检验，不能称为统计上显著。

## 完成的修复

- archive/code/run_task2_model_based.py、archive/code/dexposure_fm/macroprudential_tools.py、cloud/train_src/dexposure_fm/macroprudential_tools.py 的损失上限规则一致。
- 每个债权节点的传入累计损失均限制在 max(0, TVL)，包含零值。TVL 为零的节点仍保留在边份额分母中，但不能累计或继续传播损失。被截掉的损失不重新分配。
- 这是统一的模拟假设。原有缺失值转零逻辑未改变，不意味着缺失 TVL 在经济上就等于零。
- 基线、预测图、真实图全部重算；各 horizon 的最差 20% 按修复后的基线误差重新选择。模型原始节点与边预测保持不变。
- Figures 2–3 的误差单位明确为百分点，并修复共享坐标轴被最后一个子图范围覆盖、导致柱子截断的问题。Figure 3 的柱子是各情景内部最差 20%，注释与 Table 4 是合并情景后的最差 20%。

## Table 4：全量共同节点集

107 个预测时间点，3 种冲击，共 321 场景。ΔMAE = 基线 MAE − 模型 MAE，正数表示模型误差更低。误差单位为系统损失百分点。

| Horizon | 场景数 | 基线 MAE | 模型 MAE | ΔMAE 全体 | ΔMAE 最差20% | 尾部胜率 |
|---|---:|---:|---:|---:|---:|---:|
| h=1 | 96 | 0.352 | 0.467 | -0.116 | +0.173 | 70.0% |
| h=4 | 87 | 0.437 | 0.531 | -0.094 | +0.221 | 61.1% |
| h=8 | 75 | 0.612 | 0.625 | -0.013 | +0.625 | 93.3% |
| h=12 | 63 | 0.584 | 0.737 | -0.153 | +0.011 | 76.9% |

## 论文 Table 4 仅供参考

修复也改变了真实网络的模拟目标和基线损失，所以不能将新数值与论文旧数值作同口径性能排名。旧论文 Task II 的逐周预测、命令与 checkpoint 映射未恢复。Table 3 与 Figure 5 不调用这段传染损失代码，本次没有改变这两个结果。

| Horizon | 论文 ΔMAE 全体 | 论文 ΔMAE 最差20% | 论文尾部胜率 |
|---|---:|---:|---:|
| h1 | -0.950 | +3.040 | 100% |
| h4 | -1.710 | +1.420 | 83% |
| h8 | -0.170 | +2.600 | 100% |
| h12 | -0.550 | +2.210 | 83% |

## 采样敏感性检查

额外沿用旧脚本的每个 horizon 随机抽 10 个时间点、基线保留原节点集的设置。此设置同时改变样本和基线节点范围，不能把与全量结果的差别完全归因于采样。

| Horizon | 模型 MAE | 基线 MAE | ΔMAE 最差20% | 尾部胜率 |
|---|---:|---:|---:|---:|
| h=1 | 0.394 | 0.300 | -0.083 | 66.7% |
| h=4 | 0.526 | 0.395 | +0.120 | 50.0% |
| h=8 | 0.642 | 0.466 | +0.044 | 66.7% |
| h=12 | 0.775 | 0.703 | +0.721 | 100.0% |

h1 在这个旧抽样设置的尾部平均优势为负，说明尾部结论依赖明确的评估样本与节点范围。主表采用全部共同节点样本，不因哪种设置的分数更好而选择口径。

## 验证与文件

- 输入数据和全部 107 个 NPZ 的 SHA-256 与冻结 run 清单一致；旧审计及旧诊断的文件校验通过，未覆盖旧结果。
- 441 个场景（三图共 1323 次模拟）完成；系统损失均有限且在 0–100% 内。
- 全部预测图与真实图的新损失，与上一轮独立诊断的统一上限函数逐项一致，容差 1e-9 个百分点。所有主表 MAE、ΔMAE 按保存场景重新计算核对，容差 1e-12。
- paper/tests/test_contagion_zero_tvl.py 对三个模拟器入口逐一检查 TVL 为 0、1e-9、1e-6、0.004、5、100 和全零网络。见 test.log。
- figures/gen_fig_contagion.py 在保存前检查每个柱子都在坐标范围内，见 plot_verification.json。PNG/PDF 分别保存，图像另行人工查看。
- summary.json、table4.csv：主结果和口径。paper_common_nodes_all/ 与 legacy_default10/：全部场景损失与统计。
- evaluation_source/：数值重算时源码与执行脚本，已对照 summary.json 中记录的 SHA-256 验证；recompute.py 后续仅调整绘图路径与标题。source_fix.patch：当前修复差异。
- figures/fig_contagion_comparison.pdf、.png：Figure 2。figures/fig_contagion_advantage.pdf、.png：Figure 3。论文原文件未替换。

## 使用边界

保存预测仍使用真实正边加采样负边的候选集合，并在共同节点集评估；这不是全节点对部署测试。边金额的线性尺度长尾问题与 Figure 5 的偏差没有因为本次模拟器修复而消失。此次结果支持先修正评估再讨论训练，不能称为论文全部数值已经复现。
